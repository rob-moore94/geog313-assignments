## Reflection - Assignment 3 - Rob Moore

My code was similar to but distinct from the agentic code in several noatable patterns. First, the agentic ai code added very little annotation to the script it was producing. It was providing context in the the chat window that may be comparable to what I would want in an annotation but obviously this is not a good way to describe and keep record of what are you are doing with each block of code. Granted, I did not add any instruction to heavily annotate the code but I suppose I was expecting this to be a given. Second, the structure of the code was notably different. All helper functions were grouped togehter in the beginning of code followed by the tables to print functions that all started with the a nice 'print' prefix. The agentic code also packed all of my loops in to the if __name__ == "__main__": code more efficiently that I did. I had to use the if __name__ == "__main__": line twice to trigger seperate loops. Meaning the agentic code was able to take each objective and modify and optimize the code it had already written to be more efficient.

My initital understanding of the one of the instructions was to add the test suite to the distance.py file, not as is own python file. Thus I added an instruction into my AGENTS.md file to explicitly add to the distance.py file. However, I had an issue when prompting agent to create the test suite. It produced the correct code, but created a new file which contradicts the instruction I had added to the AGENTS.md file to only add to the distance.py. This instruction was a misunderstanding from myself of the instructions but regardless, I explicitly instructed it to not create new files. Perhaps, my prompt contradicted the AGENTS.md file thus resulting in a deviation from its instructions? I ulitmately instructed the agent to seperate the distance.py file into the distance functions and output codes and the test suite test_distance into their own respective files to match the deliverables of the assignment better.

I did not make any edits to the AGENTS.md file after its initial draft. The single deviation of the agent from the AGENTS.md file was easily correctable with a prompt, as well as my statement in my AGENTS.md file is explicit so I was unsure how to tighten to alter the behavior of the agent overall. Perhaps in the beginning of a project it is better to explictly tell it to reference AGENTS.md along every step of the project. However, it makes me uneasy to give an agent anything but highly restrictive constraints and environments to work in.
